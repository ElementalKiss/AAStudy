#include "item_02.h"
#include "item_02_Util.h"
using namespace item_02_Util;

void item_02::Execute()
{
	/*vector<Widget> vw;
	Widget bestWidget;
	bestWidget.SetX(2);
	bestWidget.SetY(3);

	vector<Widget>::iterator iter;
	//iter = find(vw.begin(), vw.end(), bestWidget);*/

	//typedef vector<Widget> WidgetContainer;
	//typedef WidgetContainer::iterator WCIterator;
	//WidgetContainer vw;
	//Widget bestWidget;
	//bestWidget.SetX(2);
	//bestWidget.SetY(3);

	//WCIterator iter;
	////iter = find(vw.begin(), vw.end(), bestWidget);

	//typedef vector<Widget, SpecialAllocator<Widget>> WidgetContainer;
	//typedef WidgetContainer::iterator WCIterator;
	//WidgetContainer vw;
	Widget bestWidget;
	bestWidget.SetX(2);
	bestWidget.SetY(3);

	/*WCIterator iter;
	iter = find(vw.begin(), vw.end(), bestWidget);*/
}